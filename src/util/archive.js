/**
 * 归档模块
 * 
 * 归档模块通过从配置中读取归档的类型、归档类型、归档时间、
 * 归档组织方式等来进行归档处理
 */
module.exports = exports = {
	init: (config) => {

		if (!config) return; // 如果没有配置，则直接终止所有的配置

		const archive = {};

		return archive;
	}
}
